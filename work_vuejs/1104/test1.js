// const classes = `header ${
//   isLargeScreen() ? "" : `icon-${item.isCollapsed ? "expander" : "collapser"}`
// }`;

// console.log(item);

console.log(item);

var item = {
  isCollapsed: true,
};

// function isLargeScreen() {
//   return false;
// }

// console.log(classes);
